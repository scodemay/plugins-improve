apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration
leaderElection:
  leaderElect: false
clientConnection:
  kubeconfig: "/home/chenyu/.kube/config"
profiles:
  - schedulerName: hello-scheduler
    plugins:
      score:
        enabled:
        - name: TinyScheduler
          weight: 100
